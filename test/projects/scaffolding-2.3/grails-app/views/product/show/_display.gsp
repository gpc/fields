<g:if test="${value}">
    <li class="fieldcontain">
        <span id="${name}-label" class="property-label">${label}</span>
        <span class="property-value" aria-labelledby="${name}-label">${value}</span>
    </li>
</g:if>